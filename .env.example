DATABASE_URL="postgresql://user:pass@host/neondb?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://user:pass@host/neondb?sslmode=require"
NEON_BRANCH="production"
AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
BLOB_READ_WRITE_TOKEN=""
CRON_SECRET="replace-with-a-long-random-string"
YATHARTH_DEPLOYMENT_ID="yatharth-erp-dev"
SETUP_SECRET="replace-with-a-long-random-string"
# Neon: run `neon link` + `neon env pull` to populate DATABASE_URL* and NEON_BRANCH.
# Project: Yathartha foods (solitary-pine-12141341), org rkyves (org-plain-sound-63905032)
# Website sync secret is stored in Settings (AppSetting), not here.
# Generate production values: powershell -File scripts/Generate-ProdSecrets.ps1
